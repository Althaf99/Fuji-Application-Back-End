--create table if not exists raw_material_vendor (
--    id bigserial primary key,
--    name varchar(255) not null,
--    location varchar(255), contact_person varchar(255), contact_number varchar(255),
--    bank_name varchar(255), bank_account_number varchar(255),
--    created_at timestamp not null, updated_at timestamp not null
--);
--create table if not exists raw_material (
--    id bigserial primary key, name varchar(255) not null, code varchar(255) not null,
--    type varchar(255) not null, color varchar(255), size varchar(255), price numeric(19,6) not null,
--    vendor_id bigint not null references raw_material_vendor(id), created_at timestamp not null, updated_at timestamp not null,
--    constraint uk_raw_material_code unique (code)
--);
--create table if not exists master_batch (
--    id bigserial primary key, name varchar(255) not null, code varchar(255) not null,
--    type varchar(255) not null, color varchar(255), size varchar(255), price numeric(19,6) not null,
--    vendor_id bigint not null references raw_material_vendor(id), created_at timestamp not null, updated_at timestamp not null,
--    constraint uk_master_batch_code unique (code)
--);
--create table if not exists grn (
--    id bigserial primary key, grn_number varchar(255) not null unique, date date not null,
--    vendor_id bigint not null references raw_material_vendor(id), purchase_order_id bigint, notes text,
--    created_at timestamp not null, updated_at timestamp not null,
--    constraint uk_grn_number unique (grn_number)
--);
--create table if not exists grn_item (
--    id bigserial primary key, grn_id bigint not null references grn(id) on delete cascade,
--    item_type varchar(30) not null, item_id bigint not null, quantity numeric(19,6) not null check (quantity > 0),
--    unit_price numeric(19,6), created_at timestamp not null
--);
--create table if not exists consumption (
--    id bigserial primary key, consumption_number varchar(255) not null unique, date date not null,
--    item_type varchar(30) not null, item_id bigint not null, quantity numeric(19,6) not null check (quantity > 0),
--    machine_no varchar(255) not null, mold_no varchar(255) not null, used_by varchar(255) not null, notes text,
--    created_at timestamp not null, updated_at timestamp not null,
--    constraint uk_consumption_number unique (consumption_number)
--);
--create index if not exists idx_raw_material_vendor on raw_material(vendor_id);
--create index if not exists idx_raw_material_code on raw_material(code);
--create index if not exists idx_master_batch_vendor on master_batch(vendor_id);
--create index if not exists idx_master_batch_code on master_batch(code);
--create index if not exists idx_grn_vendor on grn(vendor_id);
--create index if not exists idx_grn_date on grn(date);
--create index if not exists idx_grn_item_type_item on grn_item(item_type, item_id);
--create index if not exists idx_grn_item_grn on grn_item(grn_id);
--create index if not exists idx_consumption_type_item on consumption(item_type, item_id);
--create index if not exists idx_consumption_date on consumption(date);